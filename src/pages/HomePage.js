import styled from 'styled-components';

const Title = styled.h1`
  font-size: 3rem;
  font-weight: normal;
`;

function HomePage() {
  return (
    <main>
      <Title>Home</Title>
    </main>
  );
}

export default HomePage;
