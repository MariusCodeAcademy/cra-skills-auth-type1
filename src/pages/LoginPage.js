import styled from 'styled-components';

const Title = styled.h1`
  font-size: 3rem;
  font-weight: normal;
`;

function LoginPage() {
  return (
    <main>
      <Title>Login</Title>
    </main>
  );
}

export default LoginPage;
