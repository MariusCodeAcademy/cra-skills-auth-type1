import styled from 'styled-components';

const Title = styled.h1`
  font-size: 3rem;
  font-weight: normal;
`;

function AddPage() {
  return (
    <main>
      <Title>Add</Title>
    </main>
  );
}

export default AddPage;
